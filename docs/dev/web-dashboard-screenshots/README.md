# Web Dashboard — Per-Phase Screenshots

Each phase that meaningfully changes the dashboard's visual contract drops one viewport-1440
screenshot of `#/inbox` here. The Phase 0 "before" reference is Trey's 2026-05-12 screenshot
captured before any structural fix landed.

Naming convention: `phase-N-inbox.png` (or `phase-N-<view>.png` for views other than Inbox).
