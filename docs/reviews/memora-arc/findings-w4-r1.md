# Findings triage — W4 four-lane fusion, round 1

Diff: `01ebc1c` in worktree `codex-20260711T155101Z_6f69ac`. Reviewers: coordinator riskiest-read (RRF core + budget clamp — formula exact, cue collapse pre-scoring, saturating budget edge safe; calibration weakness flagged pre-review), Cursor (cursor-1) + Luna high (codex-1), both W4-worktree registry. Convergent PASSES worth recording: API-hook invariance (legacy forced for api provider; startup/desk untouched), one-embedding invariant (pinned by test), RRF math + cue collapse, substrate eligibility (duplicates rather than contradicts the W3 predicate; merge-staged excluded by status). Verdicts: both FINDINGS — all in the timeout/latency architecture, none in the fusion semantics.

| # | Sev | Source | Finding → fix contract |
| --- | --- | --- | --- |
| W4-F1 | BLOCKER | L (sharper) + C convergent | FTS lane inside four-lane runs UNBOUNDED (`hybrid.rs:181`), and the post-timeout fallback re-invokes the same unbounded path — a hung index query hangs `search` forever. **Fix:** the FTS lane and the fallback FTS both run under the same deadline machinery as the vector lanes; deadline exceeded → return the documented degraded result (empty/partial with marker), never hang. Test: mock slow index → search returns degraded within the envelope. |
| W4-F2 | HIGH | C+L | Lanes run SEQUENTIALLY with cooperative timeouts — worst case sums to ~embed+FTS+3×lane-timeouts, and `tokio::time::timeout` doesn't cancel `spawn_blocking` (a hung lane retains the index lock and queues the rest). **Fix:** run the four lanes CONCURRENTLY (join with per-lane budgets) under one overall post-embed envelope = remaining hook/search deadline; stragglers are abandoned (their results dropped; document that the blocking thread parks until the query returns — connection-mutex caveat noted in code). Wall-clock target: max(lane budgets), not sum. |
| W4-F3 | HIGH | C+L + coordinator pre-flag | `measured_fusion_reserve()` calibrates EMPTY fusion (~µs) — the reserve is decorative, and nothing bounds the post-embed phase against the 800ms hook deadline. **Fix:** keep the embed-budget formula but enforce the real envelope: after embed, the remaining deadline bounds the concurrent lane phase (per W4-F2) + fusion + hydration; envelope exceeded → fuse what's ready or degrade to FTS-only-with-marker. Calibration comment downgraded to advisory. Test: reserve ≥ deadline edge (already safe via saturating_sub — pin it). |
| W4-F4 | MEDIUM | L | Aux-only hydration copies `frontmatter.summary` without checking envelope content class — a stale-index race during encryption could surface an encrypted memory's metadata into recall. **Fix:** hydrate content only from `Plaintext` envelopes; `Ciphertext`/`MetadataOnly` hits are dropped from fusion output (fail-closed) with a debug log. Test. |
| W4-F5 | MEDIUM | C | Hook invariance is unpinned (no test that API delta stays legacy/two-lane under default four_lane_enabled), and `--fusion` writes a DUPLICATE top-level `recall:` YAML key in scaffold config (last-wins luck). **Fix:** pinning test for the API-hook invariance; scaffold writes one merged recall key. |
| W4-F6 | MEDIUM | C+L | Test gaps: RRF golden-value test covering all four weighted terms; preserved raw cue ranks after collapse; hung-FTS deadline test (part of F1). **Fix:** add them. |

Rejected: none. Round 2 = scoped re-review of the fix diff (cap 3).
