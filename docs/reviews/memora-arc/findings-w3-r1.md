# Findings triage — W3 merge-on-dream, round 1

Diff under review: wave commit `0f680f3` in worktree `codex-20260711T081806Z_5ac734`. Reviewers: coordinator riskiest-read (apply/rollback/supersede/activate core in `dream/merge.rs`), Cursor safe (cursor-1, W3-worktree registry), **Muse Spark 1.1** (opencode-1 — DP6 slot, first arc outing). Coordinator gates green pre-review (clippy ×2; governance 55/0; memoryd 1138/0; substrate 423/0).

Verdicts: Cursor FINDINGS (3 HIGH, 6 MAJOR, 1 MINOR); Muse FINDINGS (1 HIGH, 1 MAJOR, 3 MINOR + verified negatives). Convergent on quarantine stranding, doctor semantics, and the central-predicate gap. Muse independently verified the availability-gap invariant at all five crash points, the idempotency dedup keys, the journal matrix (incl. the valid-parse-truncation edge → fail-closed, safe), and reconcile-before-socket ordering — convergent negative evidence.

## Accepted findings

| # | Sev | Source | Finding → fix contract |
| --- | --- | --- | --- |
| W3-F1 | HIGH | C | Activation failure after all supersedes has NO exit: error propagates as `Other`, proposal stays `Applying`, reconcile retries forever, sources+replacement both unservable indefinitely. **Fix:** persist a reconcile-attempt counter; transient activation errors retry via reconcile; after 3 failed reconcile attempts (or a non-retryable CAS failure) the proposal moves to `Quarantined` with a notification — landing in the F2 operator exit. Test: activation forced to fail non-retryably → Quarantined, not infinite Applying. |
| W3-F2 | HIGH | C+M | Quarantine strands superseded sources; `review merges reject` only flips proposal status — the spec's named operator exit does not repair. **Fix:** reject on a `Quarantined` (or stuck) proposal performs a safe restore: every captured source still `superseded` with `superseded_by == [replacement]` gets its STATUS FIELDS restored (status/trust/superseded_by; `updated_at` bumped) while the CURRENT body is preserved verbatim (never overwrite content — that was the quarantine trigger); staged replacement tombstoned if still non-servable; proposal → RolledBack with event. Tests: quarantined proposal with one stranded+human-edited source → reject → source servable, edited body intact, replacement tombstoned. |
| W3-F3 | HIGH | C | `server.rs:71-73` logs reconcile failures and serves anyway — avoids the June crash-loop (right instinct) but leaves an unreconcilable half-applied merge servable-adjacent with no state change. **Fix:** a proposal whose reconcile fails is quarantined on the spot (status write is a local file op) + notification; serving continues (never crash-loop); only if even the quarantine write fails do we log-and-serve. Test: reconcile with a poisoned proposal → proposal Quarantined, daemon serves. |
| W3-F4 | MAJOR | C | `count_candidate_attention` (startup recall) counts merge-staged replacements → "N items require review" leaks staged presence through a spec-gated lane. **Fix:** the pending-attention counter must exclude merge-staged rows (discriminator: candidate-status rows lacking `requires_user_confirmation`, or the staging policy marker if indexed); test asserts a staged replacement does not inflate the count. |
| W3-F5 | MAJOR | C (coordinator-adjudicated; Muse's contrary trace missed that `classify_plaintext_memory` ignores `frontmatter.sensitivity`) | The source-sensitivity floor lands in frontmatter but not the persisted `ClassificationOutcome` (re-derived from content scan only). Live blast radius small (fence excludes encrypted tier; `require_same` forces equal source sensitivities) but the spec's `stricter(rebound, strictest source)` contract is not implemented at the outcome layer. **Fix:** staged write's outcome = stricter(rebound outcome, tier of max source sensitivity), implemented explicitly; test with public-scanning content in internal-sensitivity sources → outcome is internal-tier. |
| W3-F6 | MAJOR | C | Cosine similarity is never re-validated at apply — vectors can drift between propose and approve; a below-threshold pair still applies. **Fix:** preflight recomputes pairwise cosine over CURRENT abstraction vectors; missing vector or below threshold → proposal Invalidated. Test: vector re-embedded to dissimilar between propose and approve → Invalidated. |
| W3-F7 | MAJOR | C+M | Doctor fatals on ANY `Applying`, racing a healthy in-flight apply; spec says stuck-Applying OLDER than one reconcile cycle. **Fix:** fatal only when the proposal's journal mtime exceeds a reconcile-cycle threshold; fresh Applying is reported informationally. Test both sides. |
| W3-F8 | MAJOR | C | Spec §3.3 lifecycle matrix rows not substrate-enforced (`frontmatter/validate.rs` untouched): candidate→active is legal without the staging marker, pinned-demotion and rollback-restore transitions unchecked outside merge.rs. **Fix:** add the §3.3 transition validations to the substrate validation layer with tests (candidate→active requires `merge-staged-v1` provenance; illegal transitions typed-refused). |
| W3-F10 | MAJOR | M (Cursor filed MINOR — Muse's framing accepted) | `Frontmatter::is_merge_non_servable` exists but no read lane uses it; non-servability holds only via incidental per-query `status IN ('active','pinned')` filters. Spec §3.1: predicate implemented ONCE, applied by every gated lane. **Fix:** single-source the rule — a shared SQL fragment const (W2 predicate-pin pattern) used by the gated index queries + the Rust predicate for non-SQL paths; per-lane invisibility tests (FTS, vector, hybrid, startup, delta, review queue). |
| W3-F9 | MAJOR | C | Gated-lane serving invariant under-tested: only one forced-FTS assertion post-crash; no delta/startup/pending-attention assertions. **Fix:** extend the crash tests to assert invisibility across the gated lanes (folds in F4's test). |
| W3-F11 | MINOR | M | Staging creates abstraction/cue aux rows (upsert syncs unconditionally) — spec §3.6 says staging creates none, and W2's §A3 matrix implies aux state only materializes in the servable set. **Fix:** aux sync respects servable-set membership on create (no aux rows/jobs for candidate-status writes); enter-servable materializes them at activation (already W2 behavior). Test: staged replacement has zero aux rows/jobs; activation creates them. |
| W3-F12 | MINOR | M | `require_same` sensitivity fence is stricter than the spec's no-downgrade compatibility rule — over-rejects valid merges (safe direction, but a conformance gap that suppresses candidate yield). **Fix:** implement spec-compatible no-downgrade compatibility; keep the floor at max. |
| W3-F13 | NIT/doc | M | Export (`query_recall_index_including_metadata_only`, default statuses) includes staged replacements. Export is a backup surface — excluding rows from backup would be worse — and the spec gates recall/search/review-queue, not export. **Fix:** documentation note at the export site stating merge-staged rows are deliberately included (backup completeness), so the inclusion is a decision, not an accident. |

## Rejected (with reasons)

- **Muse HIGH "PrivacyNamespace::Agent under-classifies Me/Project scopes in the dual-classify"** — REJECTED: scanner-vs-floor conflation, mirror image of the June review-reject bug. Scanner SPANS raise the storage action above any namespace floor — an SSN in an abstraction classifies EncryptAtRest under the Agent floor too, and drops. Probing under the memory's real namespace would resurrect W2-F1's dead branch: under the Me floor both texts always classify EncryptAtRest and the strictness COMPARISON goes blind (that exact bug was the W2 BLOCKER, fixed by this probe design; precedent `handlers/review.rs:187-206`). Additionally the Me case is unreachable in W3: Me-floored memories are encrypted-tier and fence-excluded from candidacy; Project's floor equals Agent's. Muse's "floor-after-rebind order correct" trace for the outcome layer is superseded by W3-F5's adjudication.

## Verified-negative record (both lanes)

Availability-gap invariant holds at all five crash points (no double-recall state exists at any journal boundary); supersede/staging/activation idempotency keys correct; proposal exclusivity holds at generation AND apply; journal matrix implements the ratified torn-tail rule with the valid-parse-truncation edge failing closed; reconcile ordering precedes socket bind; crash tests fail on a no-op reconcile; journal tests exercise the real framing code.

Fix rounds: Devin A (F1–F4, F7 — operational core) then Devin B (F5, F6, F8, F10–F13 + F9 tests). Cap: 3 review rounds; this is round 1.

## Round 2 — Cursor (cursor-2) + Muse (opencode-2) on the fix layer (`b50211b` + fix-B commit)

Verdicts: both FINDINGS. Convergent BLOCKER; 5 accepted total.

| # | Sev | Source | Finding → fix contract |
| --- | --- | --- | --- |
| W3-R2-1 | BLOCKER | C+M convergent | Post-activation reject double-serves: with the replacement Active (`merge-applied-v1`) and the proposal still Applying/Quarantined, `safe_restore_from_reject` restores sources (the §3.3 merge-reject allowance helps it) while the non-servability-fenced tombstone skips the Active replacement → sources AND replacement servable. **Fix:** `safe_restore_from_reject` fences on replacement state FIRST: replacement servable (Active/Pinned, non-staging policy) → NO restore; the merge has factually applied — complete the bookkeeping idempotently (proposal → Applied, emit-once event, journal `done`) and return a typed "already applied; reject unavailable" outcome. Restore path runs only when the replacement is provably non-servable or absent. Tests: reject after AfterActivation crash → proposal Applied, sources stay superseded, replacement stays Active; reject on a pre-activation quarantine still restores. |
| W3-R2-2 | HIGH | C | No-downgrade sensitivity fence compares only against the FIRST source — `[public, internal]` passes while `[internal, public]` fails (order-dependent eligibility). **Fix:** order-independent set rule (compare min/max of the set once). Test with both orderings. |
| W3-R2-3 | MAJOR | C | Doctor stale-Applying check: missing journal mtime falls back to `now` → an Applying proposal with a deleted/absent journal is永 "fresh", never fatal. **Fix:** missing journal for an Applying proposal = STALE (fatal). |
| W3-R2-4 | MAJOR | M | `pinned→superseded` transition allowed for ANY actor (only the trust-still-pinned pair is refused) — normal supersede can silently demote a pinned memory; spec reserves pinned supersession for merge apply with a named approval. **Fix:** require actor `memoryd-merge` for pinned→superseded; other actors typed-refused. Audit shipped flows for legitimate pinned supersession (governance supersede of pinned — if the shipped CLI allows it deliberately, the row must allow that actor too; state what you found). |
| W3-R2-5 | LOW | M | Retry-counter save failure (disk-full class) loses the increment → infinite retry. ACCEPTED-AS-DOCUMENTED: disk-full is doctor territory; a comment marks the residual. |

Muse verified-negatives: retry counter otherwise correct (persisted, Retryable-only increments, threshold quarantine); §3.3 candidate→active rows cover both shipped flows (dream review-approve actor verified by coordinator on disk pre-review); single-sourced predicate agreed between SQL and Rust twins. Round 3 (cap): must-be-dry verify after Devin round C.
