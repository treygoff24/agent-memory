# Findings triage — W2 abstraction/cue substrate, round 1

Diff under review: wave commit `5667eea` (122 files, +2256/−67) in worktree `codex-20260711T044830Z_1e145a`. Reviewers: coordinator riskiest-read, Cursor safe (cursor-1, W2-worktree registry), Luna high safe (codex-2, W2-worktree registry). Coordinator gates re-ran green before review (clippy ×2 clean; substrate 420/0; memoryd 1099/0).

Coordinator riskiest-read (pre-review): §A4 order in `api/write.rs` correct (normalize→classify-enforce→validate-final); aux fence helpers all txn-scoped in `upsert.rs`; `aux_vector_table_name` length-prefixes row kind into the digest with a pinning test; `unicode-casefold` full folding with ß/İ pinned; `mark_old_superseded` now classifies instead of hardcoded Trusted.

Verdicts: Cursor FINDINGS (1 BLOCKER, 2 HIGH, 2 MAJOR, 2 MINOR, 1 NIT); Luna FINDINGS (1 BLOCKER, 2 HIGH, 2 MAJOR, 1 MINOR). Convergent on 4.

## Accepted findings (fix round 1 scope)

| # | Sev | Source | Finding → fix contract |
| --- | --- | --- | --- |
| W2-F1 | BLOCKER | C (coordinator-verified on disk at `dream/abstraction_compile.rs:171-191`) | Generation dual-classify drop is DEAD for `Scope::User`: the Me namespace FLOORS both body and combined at `EncryptAtRest`, so `combined==EncryptAtRest && body==Plaintext` never fires — a sensitive cue on a user-scoped memory survives into plaintext frontmatter. Same root pathology as the June review-reject bug (namespace = tier floor, not scanner strictness; see `handlers/review.rs:187-206`). **Fix:** probe strictness-caused-by-fields under `PrivacyNamespace::Agent` (lowest floor) for BOTH body-only and combined texts — drop the generated fields when probe-combined requires encryption (or refuses) while probe-body is plaintext-storable. Secret refusal stays absolute (either probe or real-namespace classification refusing storage → whole item refused, no disk effect). The persisted write's classification continues to use the memory's REAL namespace. Tests: user-scoped benign-body + sensitive-cue → fields dropped, body persists; user-scoped benign cue → fields kept; secret cue user-scoped → refused before any disk effect. |
| W2-F2 | HIGH | C+L (coordinator-verified: `index/query.rs:610-658` — hash check + vector write precede the meta txn; job DELETE has NO content_hash predicate) | Aux stale-fence TOCTOU: worker embeds H1, indexer commits H2 + replaces job, worker writes stale H1 vector+meta and deletes the H2 job — fresh content never embedded until reconcile. **Fix:** perform hash re-validation, vector write, meta upsert, and job delete inside ONE transaction, and scope the job DELETE with `AND content_hash = expected` (aligning with the chunk lane). Test: interleaved hash change between candidate fetch and `update_aux_embedding` → `StaleAux`, replacement job survives. |
| W2-F3 | HIGH | C+L (`embedding/worker.rs:347-401`) | Aux drain starvation: `drain_aux_batch` runs only when chunk queue is empty or after a successful batched chunk write — retry-exhausted chunk jobs, individual-embed fallback, and arity-mismatch early returns all skip aux; a poisoned chunk head starves the aux backlog forever. **Fix:** aux drain must execute on every drain pass regardless of the chunk-path outcome (all exits except hard provider errors). Test: exhausted/poisoned chunk jobs + pending aux jobs → aux drains. |
| W2-F4 | MAJOR | C+L (`embedding/worker.rs:431-432`) | Aux embedding runs blocking inference inline on the tokio runtime; chunk path uses `spawn_blocking`. **Fix:** route aux `embed_documents` through `spawn_blocking` identically. |
| W2-F5 | MAJOR | C (`tests/vector_lifecycle.rs:120-133`) | Status-transition matrix test omits `Archived` (spec §A3 row includes archive as leaving the servable set). **Fix:** add Archived to the matrix assertions (and audit for any other leaves-servable status not exercised). |
| W2-F6 | MAJOR | L (sharper) + C (`tests/index_migration_v6.rs`) | Migration test is structurally green: `open_index` creates v6 tables via current `SCHEMA_SQL`, the test drops them from an EMPTY db, and reopening recreates them through `SCHEMA_SQL` — `migrate_v6` itself is never meaningfully exercised, and no data rides through. **Fix:** build a genuine schema-5 database WITH representative rows (schema-5 DDL fixture or migration-path construction), open through the migration, assert v6 tables exist AND pre-existing data intact; rollback leg restores the pre-migration file copy and asserts readability. Must not depend on SCHEMA_SQL pre-creating v6 tables. |
| W2-F9 | MAJOR | L (`index/query.rs:54-63` + `dream/abstraction_compile.rs:49-59`) | Encrypted/metadata-only active memories are selected as compile candidates every run and skipped every run (legacy `read_memory` skips encrypted) — permanent selected-but-never-processed churn. **Fix:** candidate query excludes encrypted/metadata-only rows; job report states the exclusion policy explicitly. |
| W2-F7 | MINOR | C (`index/upsert.rs:280-294`) | One-directional `source_body_hash` freshness semantics implemented but untested. **Fix:** test body-only edit keeps mint-time hash; abstraction re-mint refreshes it. |
| W2-F8 | NIT | C (`index/query.rs:568-571`) | Cue `target_id` parsed with first `:` instead of the spec-pinned rightmost. **Fix:** `rsplit_once(':')`. |
| W2-F10 | RECONCILIATION | coordinator | `spec_coverage_manifest.rs:8` pins SPEC_PATH to v1.1; main now carries `stream-a-core-substrate-v1.2.md` with 11 per-signal (v1.2) acceptance bullets. **Fix (post-rebase):** re-point SPEC_PATH to v1.2 and add manifest entries binding each of the 11 new signals to its concrete test (including the tests added by this fix round). |

## Rejected (with reasons — no silent drops)

- **Luna BLOCKER "substrate write path does not classify the combined payload" (`api/write.rs`)** — REJECTED as a misread of the layering. Stream A never scans content by design: classification is a `ClassificationOutcome` SUPPLIED per-write by the daemon layer (repo invariant #2 "no defaults", Stream A §8.7); the substrate's job is order + consistency enforcement (`enforce_plaintext_classification`, sensitivity-mismatch refusal), which the wave correctly reordered per §A4. The §A4 sentence about `api/write.rs` mandates the ORDER reconciliation (done), not a substrate-resident scanner. Luna's scenario requires a daemon-bypassing caller, which is the substrate's documented trust model. **Retained residue:** the fix-round verify must confirm every DAEMON entrypoint's classify input includes abstraction+cues (the place the scenario would actually bite).
- **Cursor note "not one shared pipeline function"** — no fix demanded: §A4 mandates one pipeline CONTRACT over all entrypoints, not literally one function; the per-entrypoint compositions were audited by both reviewers with only the accepted findings above.

## Author-flagged residuals (accepted, tracked, no fix this round)

- Same-text explicit abstraction remint indistinguishable from body-only edit at the index seam (needs a write-intent signal through the write/index API — deliberate non-goal this wave).
- API-lane aux revocation exercised only on a synthetic local triple (no API-lane table in unit fixtures) — noted for the W4-prep vector-lane wiring, where a real API-lane fixture becomes available.

Round 2: scoped re-review of the fix diff. Cap: 3 rounds (this is round 1).

## Round 2 — Cursor (cursor-2) + Luna (codex-3) on fix commit `c84b2de`

Verdicts: both FINDINGS. 3 accepted (2 convergent), 0 rejected. All coordinator-fixed inline in `037d3ef` (test-layer + one boundary guard — zero production-logic blast radius, W0 exception pattern).

| # | Sev | Source | Finding → disposition |
| --- | --- | --- | --- |
| W2-R2-1 | MAJOR/HIGH | C+L convergent | F2 race test structurally green on pre-fix code — the TOCTOU window is unreachable through the public API post-fix, so no behavioral test can pin it. FIXED: replaced with the pinnable half (production hash-scoped delete predicate vs a fresh replacement job → 0 rows), untestability rationale written into the test. The transaction itself was inspection-cleared by both lanes. |
| W2-R2-2 | MINOR | C+L convergent | Migration test overclaimed idempotency (one call), weakened rollback (no raw-v5 read), and leaked `migrate_v6` into the public API. FIXED: moved to a `migrations.rs` unit test (export dropped), second migrate_v6 call, raw-v5 rollback assertions, Result/? idiom per the substrate boundary-check. Bonus real-world catch while fixing: **WAL checkpoint required before the pre-migration file copy** — the same hazard applies to the live-migration runbook (W5 rehearsal step must checkpoint or stop the daemon before copying). |
| W2-R2-3 | MAJOR (Luna filed HIGH; adjusted — local self-DoS during import, not remote surface) | L | Import forwarded unbounded cue arrays/abstraction strings before any validation. FIXED: count+byte caps at the import boundary (3 cues / 1 KiB each / 1 KiB abstraction); daemon caps remain the exact validator. |

Round-2 closures worth recording: Cursor recomputed all 11 manifest bullet hashes (match) and verified the manifest fails on a lost entry; F1 probe cleared explicitly (Agent/Project share the same floor — Project drop behavior unweakened; refuses_storage checked on both probe texts; probe-clean-but-Me-encrypted = benign fields inside ciphertext, not a bypass); F3 starvation pin verified to fail pre-fix; F9 `path LIKE 'encrypted/%'` justified as the repo's real discriminator (metadata_only alone insufficient because encrypted_index_projection can set metadata_only=false with a safe body).

Round 3 (scoped verify on `037d3ef`): MUST be dry — cap round.

## Round 3 — Cursor (cursor-3) must-be-dry verify on `037d3ef` — cap round

Verdict: FINDINGS — 1 MAJOR (predicate pin was a duplicated SQL literal, unbound to production: deleting the content_hash scoping from production alone would keep both the StaleAux path and the pin's copy green), 1 MINOR (migration-test relocation dropped the tag/alias/pending-jobs and post-reopen summary assertions). Import caps and manifest re-point VERIFIED-OK; WAL checkpoint explicitly endorsed as the correct operator model.

**Cap-round exception applied (written rationale, W0 precedent):** both findings are test/const-layer with zero production-logic blast radius, and both remedies were prescribed verbatim by the verify lane itself — extract the delete SQL into one shared const executed by production and test (`AUX_PENDING_JOB_HASH_SCOPED_DELETE_SQL`), and restore the dropped assertions. No judgment call was open and no production behavior changed (the const extraction moves a literal). Coordinator applied both in `701edec`; gate re-ran green (clippy ×2; substrate 423/0; memoryd 1130/0). Escalating to Trey for a 4th delegate round over a literal-to-const move would spend his attention on a decision with one possible answer.

**W2 review loop CLOSED.** Totals: 16 findings across 3 rounds (round 1: 10 accepted incl. 1 BLOCKER + 2 rejected with reasons; round 2: 3; round 3: 2), all fixed and verified; 3 author-flagged residuals tracked (same-text remint; API-lane fixture; import source-parser extraction). Wave commits: `a0429f6` (build) + `c84b2de` (fix r1) + `037d3ef` (fix r2) + `701edec` (fix r3).
