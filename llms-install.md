# Installing Memorum (agent guide)

The canonical guide for AI agents installing Memorum for a user is at:

**[`docs/agent-onboarding.md`](docs/agent-onboarding.md)**

That document covers the full detect → propose → consent → run → verify → restart loop with exact CLI commands grounded in the real `memoryd` surface.
